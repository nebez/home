[user]
	name = Nebez Briefkani
	email = me@nebezb.com
[alias]
	s = status -s
	pr = "!hub pull-request -b $1"
	last = log -1 HEAD
	proon = fetch origin --prune
	aliases = "!git config -l | grep alias | cut -c 7-"
[core]
	excludesfile = /home/nebez/.gitignore_global
	pager = diff-so-fancy | less --tabs=4 -RFX
